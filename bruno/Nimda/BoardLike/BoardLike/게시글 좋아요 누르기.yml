info:
  name: 게시글 좋아요 누르기
  type: http
  seq: 3

http:
  method: POST
  url: /api/like/board/{boardId}
  auth:
    type: bearer
    token: "{{jwtToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
