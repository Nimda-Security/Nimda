info:
  name: 유저가 받은 게시글 좋아요 개수 조회
  type: http
  seq: 4

http:
  method: GET
  url: /api/like/board/totalLikes
  auth:
    type: bearer
    token: "{{jwtToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
