info:
  name: 게시글 좋아요 조회
  type: http
  seq: 2

http:
  method: GET
  url: /api/like/board/{boardId}/likeCount
  auth:
    type: bearer
    token: "{{jwtToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
