info:
  name: 알림 삭제
  type: http
  seq: 7

http:
  method: DELETE
  url: /api/notifications/{notificationId}
  auth:
    type: bearer
    token: "{{jwtToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
