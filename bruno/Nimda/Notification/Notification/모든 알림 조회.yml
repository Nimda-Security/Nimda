info:
  name: 모든 알림 조회
  type: http
  seq: 2

http:
  method: GET
  url: /api/notifications
  auth:
    type: bearer
    token: "{{jwtToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
