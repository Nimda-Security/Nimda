info:
  name: 읽지 않은 알림 조회
  type: http
  seq: 1

http:
  method: GET
  url: /api/notifications/unRead
  auth:
    type: bearer
    token: "{{jwtToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
