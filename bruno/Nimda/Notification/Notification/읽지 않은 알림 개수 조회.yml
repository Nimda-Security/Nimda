info:
  name: 읽지 않은 알림 개수 조회
  type: http
  seq: 3

http:
  method: GET
  url: /api/notifications/hasUnread
  auth:
    type: bearer
    token: "{{jwtToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
