info:
  name: 모든 알림 읽기 처리
  type: http
  seq: 5

http:
  method: PATCH
  url: /api/notifications/readAll
  auth:
    type: bearer
    token: "{{jwtToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
