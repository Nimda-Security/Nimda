info:
  name: 알림 구독
  type: http
  seq: 4

http:
  method: GET
  url: /api/alarm/subscribe
  auth:
    type: bearer
    token: "{{jwtToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
